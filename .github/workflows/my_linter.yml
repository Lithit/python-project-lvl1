name: my_linter
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        uses: action/checkout@v2
      - name: Install dependencies  
        run: |
          pip install poetry
          make install
      - name: Run linter
        run: |
          make lint

